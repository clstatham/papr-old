graph main {
    @in {}
    @out { @dac0 @dac1 }
    #in {
        #c_amp(0.0 : 1.0) = 0.5
    }
    #out {}
    ~ {
        create @sw: sineosc;
        [@dac0 @dac1] <- sw.@out;
        sw.@amp <- #c_amp;
        sw.@freq <- @440.0;
    }
}