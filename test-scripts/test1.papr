graph test {
    @in {}
    @out {}
    #in { #in0 #in1 }
    #out { #out0 }
    ~ {
        #out0 <- (#in0 * #in1);
    }
}

graph main {
    @in {}
    @out { @dac0 @dac1 }
    #in { #ci0 #ci1 #ci2 #ci3 }
    #out { #co0 }
    ~ {
        let ts : test;
        ts.#in0 <- #ci0;
        ts.#in1 <- #ci1;
        #co0 <- (ts.#out0 + (#ci2 * #ci3));
    }
}
