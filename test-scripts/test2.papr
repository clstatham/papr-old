graph main {
    @in {}
    @out { @dac0 @dac1 }
    #in { #amp #freq }
    #out {}
    ~ {
        let s : sineosc;
        [@dac0 @dac1] <- s.@out;
        s.#amp <- #amp;
        s.#freq <- #freq;
    }
}